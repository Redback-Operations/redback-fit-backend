# Environment Variables Configuration

# Copy this file to .env and fill in the values to configure the application.

# Relational Database (SQLite)

DATABASE\_URL=sqlite:///redback.db

# OAuth Provider Credentials

oauth://

# The client ID and secret for your OAuth provider (e.g. Google, Auth0)

OAUTH\_CLIENT\_ID=<your-oauth-client-id>
OAUTH\_CLIENT\_SECRET=<your-oauth-client-secret>
OAUTH\_AUTHORIZE\_URL=<your-oauth-authorize-url>
OAUTH\_TOKEN\_URL=<your-oauth-token-url>
OAUTH\_API\_BASE\_URL=<your-oauth-api-base-url>

# Firebase Admin SDK

# Path to your service account JSON (placed under config/ and ignored by Git)

GOOGLE\_APPLICATION\_CREDENTIALS=./config/serviceAccountKey.json

# Realtime Database URL (only if you're using Firebase Realtime DB)

FIREBASE\_DATABASE\_URL=https\://<your-project-id>.firebaseio.com

# Front-End Dev Server URL (for CORS)

FRONTEND\_URL=[http://localhost:5173](http://localhost:5173)

# Flask Application Settings

FLASK\_RUN\_HOST=127.0.0.1
FLASK\_RUN\_PORT=5000

# (Optional) Flask Secret Key, used for session cookies

# If using only OAuth and no Flask sessions, you may omit

SECRET\_KEY=<your-flask-secret-key>
